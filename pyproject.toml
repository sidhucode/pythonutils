[tool.poetry]
name = "repl_python3_pythonutils-2"
version = "0.1.0"
description = ""
authors = ["MinecraftSlashe <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
inquirer = "^2.7.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
